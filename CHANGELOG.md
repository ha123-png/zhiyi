# Changelog

## 0.4.0 (Beta，候选版)

- 新增问知意：限定范围查找、统计、图表、原件对照及统一操作预览，支持确认、失效、撤销和历史复用。
- 重整数据仪表盘：全部时间范围、实际模型用量、最多四张自定义统计，图表与交互反馈更一致。
- 文件命名支持 AI 内容建议与日期_模板_序号二选一，自动采用、重试不变，原上传名可追溯；新建和复制模板默认不参与智能匹配。
- 待核对文件进入待处理事项，仪表盘直接定位校验问题；模板未保存编辑有离页保护。
- 云端上下文独立配置，修复旧版隐藏 8192 默认值；减少重复传输历史，支持短暂流式断线恢复。
- 统一 MCP/原生统计与导出状态，修复密钥撤销、Excel 表头公式、工作表名称、备份清单容量和旧文件检索问题。

- 明细默认三条并限制长字段，完整聚合不将整表塞入模型；长历史分页与按需回读，流式增量独立持久化，重放不重复文字。
- 任务区固定高度、长文件名截断且状态标签不挤压，副本处理集中在简洁弹窗；图表过程和重复文字折叠，宽表横向滚动。
- 增加千条/万条真实模型验收及连续查询、停止、重启和备份恢复检查。

## 0.3.0 (Beta)

- 模板历史可查看差异并将选定版本设为当前，不创建重复版本；拖动和键盘排序保留字段含义。
- 增加卡片阅读、来源与处理范围保留、规则正则试填和更明确的使用说明。
- 待核对来源保留到数据仓库、合并结果及 Excel / CSV / JSON；入表不再被误认为已人工核验。
- 改进本地模型启动失败提示和有界诊断详情；AI 模板输出及规则建议采用共同字段约定。
- MCP 增加任务与诊断查询；导出需要同时启用文件和数据读取权限。
- 新增隔离浏览器旅程、格式/队列压力检查及 Windows CI 构建和安装生命周期验证。
- 修复安装器误停其他目录的知意、模板保存时切换被覆盖，以及维护重启时 SQLite 连接失败的有限恢复。
- 字段类型显示中文；底层类型值与旧模板保持兼容。

真实模型及有界压力测试范围、Ollama 未实机验证等限制见 README。CI 产物不等于已签名正式发行。

## 0.2.0 (Beta)

- 新增六类引导示例和完整功能说明，强化“理解并结构化”而非仅票据提取的产品定位。
- AI 生成模板可选生成受控校验规则，展示规则含义、用途和是否进入模板；非法规则明确拒绝。
- 数据仓库支持独立编辑、字段与明细修订追踪、分 Sheet 导出及更可靠的表格导入与纵向合并。
- 文件历史补充处理模型与原文件访问，审核与任务状态刷新链路得到修复。
- MCP 改为默认关闭的细粒度权限控制，配置保存后新连接立即按最新权限执行。
- 改进模型方案、内置模板、安装生命周期和多处桌面端交互。

## 0.1.0 (Beta)

首个公开 Beta：

- Windows 本地桌面安装与单实例运行。
- LM Studio、Ollama 与 OpenAI-compatible 模型方案。
- 内置及自定义字段模板、智能匹配与人工审核。
- 数据仓库、修订记录、附加列、导入、合并和 Excel 导出。
- 文件历史、处理方式追溯、暂停/恢复与失败重试。
- 本地 API、MCP 与细粒度最小权限控制。
- 备份、恢复、升级和卸载数据保留策略。

已知限制见 README。
